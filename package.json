{
  "name": "demo-test",
  "description" : "Suite of microservices and clients to broadcast messages using MQTT and websockets.",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "devDependencies": {
    "coveralls": "^2.11.14",
    "mocha": "^3.1.0",
    "mockery": "^1.7.0",
    "should": "^11.1.0"
  },
  "scripts": {
    "test": "test.sh",
    "initialize": "install-modules.sh"
  }
}
